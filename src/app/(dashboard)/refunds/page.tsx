"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {Topbar} from "@/components/Topbar";import {api} from "@/lib/api";import {dateTime,money} from "@/lib/format";interface Row{id:string;amount:number;refundedAmount:number;provider:string;refundedAt?:string|null;user:{name:string;phone:string}}export default function Page(){const[rows,setRows]=useState<Row[]>([]);useEffect(()=>{api.get("/payments",{params:{status:"REFUNDED",limit:100}}).then(r=>setRows(r.data.items??[]));},[]);return <><Topbar title="مركز الاستردادات"/><div className="p-6"><div className="overflow-x-auto rounded-xl border dark:border-gray-800"><table className="w-full text-sm"><thead><tr><th className="p-3">الراكب</th><th>الدفعة</th><th>المبلغ</th><th>المسترد</th><th>المزود</th><th>التاريخ</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-t dark:border-gray-800"><td className="p-3">{r.user.name}</td><td><Link className="text-brand" href={`/payments/${r.id}`}>{r.id.slice(0,8)}</Link></td><td>{money(r.amount)}</td><td>{money(r.refundedAmount)}</td><td>{r.provider}</td><td>{dateTime(r.refundedAt)}</td></tr>)}</tbody></table></div></div></>}
